# Core bot dependencies (lightweight - no browser needed)
python-telegram-bot==21.10
python-dotenv==1.0.0
aiosqlite==0.19.0
httpx>=0.27.0
requests==2.31.0

# For code extraction from Omada Cloud (needs browser)
playwright==1.61.0
